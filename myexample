package mymodule //声明package名字为mymodule

import "fmt" //引入标准输出包

// 这个方法需要一个string形的变量，复制给name。这个函数的返回值为string类型
func HelloA(name string) string {
	//fmt的Sprintf函数将安装格式返回一个字符串
	//这个函数的具体用法可以在官方文档中找到哦。
	//官方解释：Sprintf根据格式说明符进行格式化并返回结果字符串。
	//https://pkg.go.dev/fmt#example-Sprintf
	// := 简短格式赋值，用法为，名字 := 表达式
	message := fmt.Sprintf("Hi, %v. Welcome!", name)
	return message
}
